name: Scrape foot-national.com (test)

on:
  workflow_dispatch:
    inputs:
      url:
        description: "URL foot-national.com à récupérer (page de résultats/calendrier)"
        required: true
        type: string

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Installer les dépendances
        working-directory: scripts
        run: npm install

      - name: Récupérer la page cible
        working-directory: scripts
        env:
          TARGET_URL: ${{ inputs.url }}
        run: node scrape-foot-national.js

      - name: Sauvegarder le HTML récupéré
        uses: actions/upload-artifact@v4
        with:
          name: page-html
          path: scripts/output/page.html
          retention-days: 7
