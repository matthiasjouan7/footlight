name: Enregistre une vidéo réseaux sociaux — alerte recrutement (lecture seule, aucune écriture)

on:
  workflow_dispatch:

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Installer les dépendances
        working-directory: scripts
        run: npm install

      - name: Installer Chromium (Playwright)
        working-directory: scripts
        run: npx playwright install --with-deps chromium

      - name: Enregistrer la vidéo
        working-directory: scripts
        env:
          OUT_DIR: ${{ github.workspace }}/demo-video-alerte-recrutement-reseaux-out
        run: node record-demo-video-alerte-recrutement-reseaux.js

      - name: Publier la vidéo en artefact
        uses: actions/upload-artifact@v4
        with:
          name: demo-video-alerte-recrutement-reseaux
          path: demo-video-alerte-recrutement-reseaux-out/*.webm
          retention-days: 7
